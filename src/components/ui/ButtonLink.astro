---
export interface Props {
  href: string;
  variant?: 'gold' | 'outline';
  size?: 'sm' | 'lg';
  class?: string;
  target?: '_blank' | '_self';
  rel?: string;
}

const {
  href,
  variant = 'gold',
  size = 'sm',
  class: className = '',
  target,
  rel = target === '_blank' ? 'noopener noreferrer' : undefined,
} = Astro.props;
const variants = {
  gold: 'bg-gold-gradient text-ink-button shadow-[var(--shadow-gold)] border-transparent hover:bg-gold-deep',
  outline: 'border-border bg-surface text-ink hover:border-primary/40',
} as const;
const sizes = {
  sm: 'min-h-10 px-5 text-sm',
  lg: 'min-h-12 px-6 text-[0.9375rem]',
} as const;
---

<a
  href={href}
  target={target}
  rel={rel}
  class:list={[
    'inline-flex items-center justify-center gap-2 rounded-[var(--radius-button)] border font-bold whitespace-nowrap transition-colors',
    variants[variant],
    sizes[size],
    className,
  ]}
>
  <slot />
</a>
